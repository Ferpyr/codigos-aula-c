#include <stdio.h>

int main() {
    //declaração de variaveis
    // Entrada
    float nota1 = 8;
    float nota2 = 7;

    //processamento
    float media = (nota1 + nota2) / 2;

    // Saída
    printf("A media é %f", media);
    return 0;
}